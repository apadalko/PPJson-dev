import os, sys
def get_folder_list(path):
    fullpath = '.%s' % path;
    subdirectories = os.listdir(fullpath)
    return subdirectories;
def get_files_in_folder(folder_path):
    print (s)

def get

